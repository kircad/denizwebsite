# denizkirca.com

Personal site. Next.js (app router) + Tailwind + Framer Motion, exported as a static site (`output: 'export'`) and deployed to Netlify from the `out/` directory.

- `npm run dev` — local dev server
- `npm run build` — static export to `out/`
